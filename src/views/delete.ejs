<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <div class="topnav">
        <!-- <%= JSON.stringify(userDelete) %> -->
        <a  href="/">Home</a>
        <a class="active" >Delete</a>
    </div>
    <div class="form-add-new">
        <form action="/delete-user" method="POST">
            <fieldset>
                <legend>Delete USER</legend>
                <div class="d-none">
                    <label for="">ID:</label>
                    <input value="<%= userDelete.id %>" type="text" name="id">
                </div>
                <div class="input-group">
                    <label for="">Are you sure delete this user:</label>
                    <input value="<%= userDelete.email %>" type="text" name="email">
                </div>
                <div>
                    <button>Submit</button>
                </div>
            </fieldset>
        </form>
    </div>
</body>

</html>